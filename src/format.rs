// handles .face data conversions

pub fn pack(w: u32, h: u32, levels: u8, step_s: f32, step_d: f32, bytes_s: &[u8], bytes_d: &[u8]) -> Vec<u8> {
    todo!()
}

pub fn unpack(bytes: &[u8]) -> (u32, u32, u8, f32, f32, Vec<u8>, Vec<u8>) {
    todo!()
}