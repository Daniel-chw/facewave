// handles .face data conversions

pub fn pack(
    _w: usize,
    _h: usize,
    _dwt_depth_s: u8,
    _dwt_depth_d: u8,
    _quantise_step_s: f32,
    _quantise_step_d: f32,
    _bytes_s: &[u8],
    _bytes_d: &[u8],
) -> Vec<u8> {
    todo!()
}

pub fn unpack(_bytes: &[u8]) -> (usize, usize, u8, u8, f32, f32, Vec<u8>, Vec<u8>) {
    todo!()
}
